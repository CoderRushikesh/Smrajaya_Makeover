import React from "react";
import "../css/ServiceHero.css";
import heroImage from "../image/services.png"; // pick your single image

const ServiceHero = () => {
  return (
    <section
      className="hero-slider"
      style={{ backgroundImage: `url(${heroImage})` }}
    >
      
      {/* jnnksad */}
    </section>
  );
};

export default ServiceHero;
